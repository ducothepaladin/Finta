# Copy to .env in the repo root to override Docker Compose defaults.
# docker compose automatically loads a root .env file.

APP_PORT=8080
CLIENT_ORIGIN=http://localhost:8080

JWT_ACCESS_SECRET=change-me-access
JWT_REFRESH_SECRET=change-me-refresh

BREVO_API_KEY=
BREVO_FROM_EMAIL=
BREVO_FROM_NAME=

FILE_SERVICE_URL=https://pdn-api-files.laconic.co.th/api/file/v1
UPLOAD_FILE_PATH=finta/documents

OPENAI_API_KEY=
AI_DEFAULT_MODEL=gpt-4o-mini
AI_INTERNAL_TOKEN=
